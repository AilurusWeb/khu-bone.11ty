<div id="navbar" class="c-navbar">
    <ul class="c-navbar-inner">
        {% for item in navbar %}

            {% liquid
                if item.slug | isCurrentPage: page.url
                    assign a_cls = "c-navbar-link is-current"
                else
                    assign a_cls = "c-navbar-link"
                endif
            %}

            <li>
                <a href="{{ item.slug }}" title="{{ item.text }}" class="{{ a_cls }}" {{ item.slug | linkActived: page.url
                    }}>{{ item.text }}</a>
            </li>
        {% endfor %}
    </ul>
</div>